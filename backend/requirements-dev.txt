# Development / testing — install with: pip install -r requirements-dev.txt
# This includes core requirements + all local OCR backends for full development setup
-r requirements.txt
-r requirements-ocr.txt
-r requirements-texify.txt
pytest>=8.0.0
