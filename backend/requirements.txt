# Core requirements (always needed)
fastapi>=0.115.0
uvicorn>=0.34.0
pywin32>=308
beautifulsoup4>=4.12.0
lxml>=5.3.0
python-multipart>=0.0.9

# Gemini API OCR (cloud-based, requires API key)
google-genai>=1.0.0
python-dotenv

# Install additional local OCR backends as needed:
# - For GOT-OCR 2.0: pip install -r requirements-ocr.txt
# - For texify: pip install -r requirements-texify.txt
# - For development: pip install -r requirements-dev.txt
